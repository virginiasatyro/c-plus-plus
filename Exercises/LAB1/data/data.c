#include "data.h"

/*Data::Data(int dia, int mes, int ano){
    _dia = dia;
    _mes = mes;
    _ano = ano;
}

int Data::get_dia(){
    return _dia;
}

int Data::get_mes(){
    return _mes;
}

int Data::get_ano(){
    return _ano;
}

std::string Data::get_data_curto(){
    std::string a = std::to_string(_dia);
    std::string b = std::to_string(_mes);
    std::string c = std::to_string(_ano);
    std::string d = "/";
    std::string e = a+d+b+d+c;

    return e;

}
std::string Data::get_data_longo(){
    std::string b = std::to_string(_dia);
    std::string c = std::to_string(_ano);
    std::string d = " de ";
    if (Data::_mes == 1)
         {std::string a = "janeiro";
             return b + d + a + d + c;
         }
    else if(Data::_mes== 2)
         {std::string a = "fevereiro";
             return b + d + a + d + c;
         }
    else if (Data::_mes== 3)
         {std::string a = "marco";
             return b + d + a + d + c;
         }
    else if(Data::_mes == 4)
         {std::string a = "abril";
             return b + d + a + d + c;
         }
    else if (Data::_mes == 5)
         {std::string a = "maio";
             return b + d + a + d + c;
         }
    else if(Data::_mes == 6)
         {std::string a = "junho";
             return b + d + a + d + c;
         }

    else if(Data::_mes== 2)
         {std::string a = "fevereiro";
             return b + d + a + d + c;
         }
    else if (Data::_mes== 3)
         {std::string a = "marco";
             return b + d + a + d + c;
         }
    else if(Data::_mes == 4)
         {std::string a = "abril";
             return b + d + a + d + c;
         }
    else if (Data::_mes == 5)
         {std::string a = "maio";
             return b + d + a + d + c;
         }
    else if(Data::_mes == 6)
         {std::string a = "junho";
             return b + d + a + d + c;
         }
    else if(Data::_mes == 7)
         {std::string a = "julho";
             return b + d + a + d + c;
         }
    else if  (Data::_mes == 8)
         {std::string a = "agosto";
             return b + d + a + d + c;
         }
    else if (Data::_mes == 9)
         {std::string a = "setembro";
             return b + d + a + d + c;
         }
    else if(Data::_mes == 10)
         {std::string a = "outubro";
             return b + d + a + d + c;
         }
    else if (Data::_mes == 11)
         {std::string a = "novembro";
             return b + d + a + d + c;
         }
    else
         {std::string a = "dezembro";
             return b + d + a + d + c;
         }

}
*/
